# build with mingw, install qt mingw tools components and add it to environment variables, and maybe other dependency exist
ndk_cmake_mingw_build.cmd
# stdc++ 
set -DANDROID_STL="none", which means not use standard c++ library

